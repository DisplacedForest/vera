{
  "kind": "media",
  "label": "Media",
  "icon": "film",
  "order": 3,
  "nominal_label": "quiet",
  "blurb": "a weekly worth-adding digest for your media library",
  "pipeline": [
    {"block": "media_candidates"},
    {"block": "llm_judge", "params": {
      "bar": "is a movie or TV title compatible with the household taste profile ({options.taste}). Judge from the title and description alone: keep it unless it violates a hard rule or is plainly filler; a later stage caps how many post"}},
    {"block": "media_digest"}
  ],
  "schedule": "0 9 * * 0",
  "requires": [
    {"kind": "feature", "integration": "overseerr", "feature": "media_curation"}
  ],
  "providers": [],
  "options": [
    {"group": "Curation", "fields": [
      {"id": "taste", "label": "Taste profile", "type": "text",
       "env": "MEDIA_CURATION_TASTE",
       "default": "Aim for a well-rounded, genuinely good collection. Raw popularity is polluted, treat it as a candidate pool, not a ranking. Choose with taste: genuine critical acclaim and real current cultural relevance over mere view counts.",
       "hint": "hard rules and preferences; this text is the curation bar"},
      {"id": "cap", "label": "Picks per digest", "type": "number",
       "env": "MEDIA_CURATION_CAP", "default": 8}
    ]}
  ]
}
