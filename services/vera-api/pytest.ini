; Makes `pytest` work from any directory without PYTHONPATH: pytest resolves this file
; as rootdir and puts services/vera-api on sys.path so `from routers import ...` imports.
[pytest]
pythonpath = .
testpaths = tests
